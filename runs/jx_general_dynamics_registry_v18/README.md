# JX General Dynamics registry v18

Status: `PARTIALLY_QUALIFIED_RESEARCH_PLATFORM`

Scientific claim state: `SCREENING_ONLY`

This create-only export binds registry generation 18 to package release
candidate `0.6.0rc3`. It advances only the lunar evidence binding: the
complete delayed-deformation and source-fixed geodetic-transport promotion
gates both failed, so the lunar capability remains `INCONCLUSIVE` and
unqualified. Its next gate is a separately preregistered simultaneous
translation/rotation model with complete delayed deformation and
common-potential reactions.

`production_ready` and `unified_multiphysics_execution` remain false. All
other capability statuses, evidence bindings, and claim ceilings are retained.
Registry v17 and all earlier exports remain immutable historical evidence.

Regenerate the live registry only into a new, empty destination with
`benchmarks/jx_general_dynamics_registry.py`.
