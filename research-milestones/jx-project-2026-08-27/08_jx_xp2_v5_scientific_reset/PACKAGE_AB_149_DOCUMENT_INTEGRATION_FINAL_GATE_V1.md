# XP2 Package A/B 149-document integration final gate v1

This is a non-authority audit checkpoint outside every accepted schema,
source, experiment, registration, and runtime root. It freezes the reconciled
structural and semantic integration verdict at `2026-08-27T12:25:18Z`. It
does not instantiate a policy, register an artifact, grant authority, build or
execute code, start dynamics, or create a scientific result.

## Final verdict

**GO — P0=0 / P1=0 / normative-P2=0.**

- `READY_FOR_REGISTRATION`: **YES**, meaning entry into the separate,
  fail-closed registration/preflight stage is permitted.
- `REGISTRATION_COMPLETE`: **NO**.
- `READY_FOR_SIMULATION`: **NO**.

The user's conditional authorization to run a later simulation does not alter
this result and does not itself create machine authority. It becomes
actionable only after the concrete policy, authority, dynamics, inputs,
execution environment, independent solver, and comparison package are all
registered, durably frozen, independently verified, and the final preflight
returns dual GO.

## Frozen integration evidence

The active ordered corpus is exactly:

1. six accepted upstream schema documents;
2. accepted Package A final20, 137 leaves / 600,746 bytes / inventory
   `841187d780ec4cd79697a642b151dce19dada085b1b92be1faa7713f010f82f3`;
3. accepted Package B correction successor, six leaves / 8,108,277 bytes /
   inventory
   `b8376a5e4d929228124b8ae1d3278a1eb2e922c01d2f921bcdf208bf0d2d1fe4`.

The resulting 149-document corpus is 9,058,795 bytes. Its canonical ordered
policy-roster subvalue is 50,365 bytes / SHA-256
`3f60f8009f719be04234cd4e6e94bdd07d59ad738e0a435d814186a8a673d54f`.
The independent audit-only live roster digest, binding each position to its
absolute path, raw size, raw SHA-256, and schema ID under the framing recorded
in the structural audit, is
`663ec6e132b5cdaf575d704d71f660182f157427409353de6dcb8f0199e9e944`.

The integration closes exactly 139 payload-matrix rows, 35 phase/grant/
predecessor/slot rows, five exact non-document sources, Package A-to-B
contracts, all explicit import and source bindings, cap and framing equality,
active V2 lineage, and an acyclic one-way dependency graph. Rejected and stale
predecessors are excluded. Position 149 remains a fixed-policy schema, not a
policy instance; no accepted leaf self-binds its own hash or a future policy
hash.

The two independent reports frozen by this gate are:

- structural audit: 6,034 bytes / SHA-256
  `5e57c4f32d4147ce5ea7d7ed95afae31bf2e5a7aede81e21ee133b5f7c418576`;
- semantic audit: 7,579 bytes / SHA-256
  `61552781cd5c0074807f3393aabdb3a3dace44512991a1450067a6cf978a47c2`.

Both independently return GO with P0=0, P1=0, and normative-P2=0. The frozen
Package B checker additionally returns zero lint errors and zero adversarial
errors on the accepted target.

## Authorized next stage and remaining prerequisites

Only registration/preflight package assembly is now authorized. Before any
compile, runtime, dynamics, or simulation action, the next stage must create
and freeze the smallest exact set of non-circular evidence for:

1. a concrete fixed-policy instance binding all 149 documents and five
   sources, plus external binding of the policy instance's own final
   path/size/hash;
2. exact retained-root identity and complete-listing evidence, immutable trust
   and issuer roots, rollback-resistant clock and ledger state, one-use
   approvals, preassigned slots, and immutable or exclusive-writer protection;
3. a CPU-only, offline execution and toolchain profile with reviewed binaries,
   parsers, loaders, libraries, headers, argv, environment, cwd, FD/map/
   process/resource closure, and output-root absence;
4. an explicitly registered synthetic/no-observed-data dynamics and input
   bundle, including equations/model, parameters, numerical method, precision,
   tolerances, seeds, checkpoints, initial states, selection assumptions,
   hashes, and claim ceiling;
5. a genuinely independent solver specification and implementation, its
   separate dependency closure, independence evidence, restart probes,
   tolerances, and comparison protocol; and
6. the staged no-dynamics validation, engineering registration, persistent
   runtime materialization, both runtime preflights, independent verification,
   final registration, and a last dual-GO activation check.

Any missing prerequisite or scope-changing choice must stop the stage for an
explicit decision. Until all are complete, every dynamics, network, GPU,
observed-data, build, execution, installation, publication, and simulation
grant remains false.

No accepted schema, source, Package A or Package B leaf, durable mirror,
runtime root, registration root, executable, build output, or simulation
output was created or changed by this gate.
