# JX-XP2 V5 operational adapter v1 -- review-only source package

Status: **NONAUTHORITY_REVIEW_ONLY**.

This directory is a separate, versioned operational-registration/adapter
source candidate. It is not part of the accepted 149-document corpus and does
not modify or supersede any accepted document, source, root, policy, or
scientific input.

The package is governed by the frozen external DAG checkpoint:

- path:
  `outputs/jx_xp2_powerloss_checkpoint_v1/XP2_V5_OPERATIONAL_REGISTRATION_ADAPTER_DAG_FREEZE_V1.md`
- raw SHA-256:
  `6112175fae2a2cfe8a6e5571a4191945fa8dd74e118fbde16045cb13f43afc47`

## Current release boundary

This source package may be reviewed as static bytes only. It does not
authorize or perform compilation, publication, registration, runtime
materialization, preflight, engineering execution, dynamics, simulation,
network access, GPU access, or observed-data consumption.

The accepted nine `/tmp` roots are intentionally unchanged. Current process
and filesystem capabilities cannot provide their required immutable/noatime/
exclusive-writer evidence or an external trust root. Consequently:

- no identity or listing leaf is asserted as authoritative;
- no concrete fixed-policy instance is published;
- no operational policy or approval is issued;
- no caller, publisher, registrar, materializer, runtime, or receipt is
  asserted as reviewed or executable; and
- every grant remains false.

The `*_v5.py` files are explicit fail-closed successor placeholders until a
complete source-level successor and exhaustive V4-to-V5 science-equivalence
proof are independently accepted. They are not wrappers and never import or
execute the V4 modules.

## Immutable scientific boundary

Any eventual successor must preserve exactly:

- the retained initial-state, seed, and selection bytes;
- all units, reference frames, bodies, masses, radii, configurations,
  orientations, tracers, force equations, omissions, and state expansion;
- MERCURIUS and audit timesteps and all integrator settings;
- DOP853 equations, tolerances, step and restart semantics;
- checkpoint science, metrics, comparison thresholds, failure mappings, and
  classification order; and
- claim ceiling
  `SYNTHETIC_1MYR_8_ORIENTATION_RESPONSE_WITH_DOP853_SENTINEL_ONLY`.

Only exact protocol identifiers, paths, inventories, runtime split,
authority/ledger rules, launch/materialization, preflight/probe/receipt, and
V4-unavailability lineage may change. A wrapper, namespace alias, runtime
constant patch, environment override, input normalization, scientific value
copy, or caller-selected parameter is forbidden.

## External prerequisites

Actual authority requires preexisting, independently supplied:

1. external authenticator/issuer/publisher/sequencer trust generations;
2. rollback-resistant clock and append-only replay ledger;
3. kernel-proven noatime and immutable or exclusive-writer generations;
4. distinct reviewed caller, publisher, registrar, materializer, executor,
   verifier, and activation roles; and
5. protected, compatible A/B/C/D runtime and toolchain roots.

A user-owned key, chmod-only file, advisory lock, serialized assertion, or
later receipt is not a substitute.
