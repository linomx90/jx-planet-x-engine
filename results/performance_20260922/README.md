# JX/REBOUND performance research — 2026-09-22

This active result bundle preserves the source-bound reports produced while
building the equal-scope raw-C JX/TRACE harness and stateful NEAR RKF78
prototype.

Scientific status: `SCREENING_ONLY`.

The reports do not establish general superiority. The JX RKF78 lane uses
`1e-13` tolerances while TRACE's close Bulirsch–Stoer lane retains its `1e-8`
default, so the comparison is not accuracy-matched. The fixtures are short
and synthetic.

Files are generated directly by their source-bound benchmark scripts. The
catalog records their identities after generation.
